import { ShorStoryPipe } from './shor-story.pipe';

describe('ShorStoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ShorStoryPipe();
    expect(pipe).toBeTruthy();
  });
});
